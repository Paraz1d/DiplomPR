<UserControl x:Class="AdminTaskSystem.Views.TaskEditDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="420">
    <StackPanel Margin="24">
        <TextBlock Text="Редактирование задачи" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16" />
        <TextBlock Text="{Binding SelectedTask.Title}" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,12" />
        <ComboBox ItemsSource="{Binding Statuses}" SelectedItem="{Binding EditStatus}" materialDesign:HintAssist.Hint="Статус"
                  Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,0,0,12" />
        <TextBox Text="{Binding EditComment, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Комментарий"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}" MinLines="3" AcceptsReturn="True" Margin="0,0,0,16" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Command="{Binding CancelEditCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,8,0" />
            <Button Content="Сохранить" Command="{Binding SaveTaskCommand}" Style="{StaticResource MaterialDesignRaisedButton}" />
        </StackPanel>
    </StackPanel>
</UserControl>
