<Window x:Class="AdminTaskSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:infra="clr-namespace:AdminTaskSystem.Infrastructure"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="AdminTaskSystem" Width="460" Height="560" WindowStartupLocation="CenterScreen"
        Background="#EEF3F8" ResizeMode="NoResize">
    <Window.DataContext>
        <viewModels:LoginViewModel xmlns:viewModels="clr-namespace:AdminTaskSystem.ViewModels" />
    </Window.DataContext>
    <Grid>
        <materialDesign:Card Width="380" VerticalAlignment="Center" Padding="0" UniformCornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="96" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Background="#0D47A1" CornerRadius="8,8,0,0">
                    <StackPanel VerticalAlignment="Center" Margin="28,0">
                        <TextBlock Text="AdminTaskSystem" Foreground="White" FontSize="24" FontWeight="SemiBold" />
                        <TextBlock Text="Муниципальная администрация" Foreground="#D6E8FF" FontSize="13" />
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="1" Margin="28" VerticalAlignment="Center">
                    <TextBox materialDesign:HintAssist.Hint="Email"
                             Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,16" />

                    <Grid>
                        <PasswordBox materialDesign:HintAssist.Hint="Пароль"
                                     infra:PasswordBoxAssistant.BoundPassword="{Binding Password, Mode=TwoWay}">
                            <PasswordBox.Style>
                                <Style TargetType="PasswordBox" BasedOn="{StaticResource MaterialDesignOutlinedPasswordBox}">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPasswordVisible}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </PasswordBox.Style>
                        </PasswordBox>
                        <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                                 materialDesign:HintAssist.Hint="Пароль"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Visibility="{Binding IsPasswordVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </Grid>

                    <CheckBox Content="Показать пароль" IsChecked="{Binding IsPasswordVisible}" Margin="0,8,0,10" />

                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#C62828" Margin="0,0,0,12"
                               Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}" />

                    <Button Content="Войти" Height="44"
                            Command="{Binding LoginCommand}"
                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                            Style="{StaticResource MaterialDesignRaisedButton}" />
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Window>
